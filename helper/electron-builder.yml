# electron-builder config for Excelsis Helper.
# Install layout:
#   resources/app.asar
#   resources/scripts/**
#   resources/build/**
#   resources/macros/**
#   resources/install-settings-preset.json
# The de-elevated app deploys bundled macros to the logged-in user's Documents
# folder once per version and backs up differing existing source macros.
appId: local.excelsis.automation
productName: Excelsis Helper
artifactName: ${productName}-Setup-${version}.${ext}
copyright: Excelsis

directories:
  output: dist
  buildResources: build

# Swaps the executable icon and strips GPU DLLs unused with hardware
# acceleration disabled in main.cjs.
afterPack: scripts/after-pack.cjs

# Production hardening. Cookie encryption and a browser-only V8 snapshot stay
# off because the app has no cookie-backed sessions and ships one shared
# snapshot. The file renderer uses only same-origin bundled assets, so it does
# not need Electron's legacy file:// fetch, service-worker, or child-frame powers.
# Electron 42's ninth WasmTrapHandlers fuse intentionally remains enabled: its
# guard-page implementation is lighter than explicit bounds checks. The release
# audit reads all nine slots because the bundled fuse library configures eight.
electronFuses:
  runAsNode: false
  enableCookieEncryption: false
  enableNodeOptionsEnvironmentVariable: false
  enableNodeCliInspectArguments: false
  enableEmbeddedAsarIntegrityValidation: true
  onlyLoadAppFromAsar: true
  loadBrowserProcessSpecificV8Snapshot: false
  grantFileProtocolExtraPrivileges: false

files:
  - main.cjs
  - preload.cjs
  - package.json
  - automation.html
  - automation.js
  - automation.css
  - icon-256.png
  - loading.html

extraResources:
  - from: scripts
    to: scripts
    filter:
      - "**/*"
      - "!after-pack.cjs"
  - from: build
    to: build
    filter:
      - icon.ico
      - icon-256.png
  - from: macros
    to: macros
  - from: install-settings-preset.json
    to: install-settings-preset.json
  - from: LICENSE
    to: LICENSE.txt
  - from: THIRD_PARTY_NOTICES.md
    to: THIRD_PARTY_NOTICES.md
  - from: licenses
    to: licenses

win:
  target:
    - nsis
  icon: build/icon.ico
  electronLanguages:
    - en-US
    - hu

nsis:
  oneClick: false
  perMachine: true
  allowToChangeInstallationDirectory: true
  deleteAppDataOnUninstall: false
  include: installer.nsh
  shortcutName: Excelsis Helper
  # installer.nsh launches after interactive installs so the optional preset
  # can fill missing settings. Silent installs do not launch.
  runAfterFinish: false
